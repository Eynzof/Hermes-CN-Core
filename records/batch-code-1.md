# Batch code-1 — conflict resolution report

Batch: `batches/code-1.txt` (33 production files). Resolved per `records/RESOLUTION-PROTOCOL.md` and `FORK_NOTES.md` (CN patches P-014…P-052). All `.py` files pass `python -m py_compile`; `apps/desktop/electron/main.ts` passes `node --check`; zero conflict markers remain in every file. `git add` done per file; **no commit** made.

| File | Decision | Rationale |
|---|---|---|
| `acp_registry/agent.json` | took-upstream (deleted) | Upstream deleted the file; already staged as `D` (no markers, no local divergence). Left as-is. |
| `agent/agent_init.py` | merged | 12 regions. R1: kept ours `_api_key_required` (P-018) + upstream's 6 new helpers + upstream `_build_codex_gpt5_autoraise_notice` signature (context_length). R2–R3, R7, R9–R10, R12, R15: upstream (new features/init blocks). R4: upstream `build_moa_facade` (API rename — shared code calls it). R5/R8/R11: upstream `load_config_readonly` (read-only load refactor). R6: upstream `resolve_entry_api_key` (secret_scope-aware, supersedes our inline env read). R12: upstream threshold-cap note. |
| `agent/agent_runtime_helpers.py` | merged | 21 regions. R1: both imports kept. R2/R11/R26: upstream lazy `%s` logging. R3–R6: upstream pattern-constant refactor (equivalent regexes). R7–R12: upstream `_rotate_failed_credential` + attribution fixes. R13: upstream `_retire_shared_openai_client` (method rename). R14/R15/R16: upstream. R19: upstream (`repair_empty_non_final_messages` + `_msg_has_payload`; imported by tests). R20: **kept ours P-024 fused single-pass `sanitize_api_messages`**, plus wired upstream's `repair_empty_non_final_messages` heal call before the fused pass so both empty-content strategies coexist. R21: upstream `_connection_candidates` (superset incl. anyio). |
| `agent/anthropic_adapter.py` | merged | R1/R3 quote-style → upstream. R2: merged ours P-022 TCP-keepalive client injection with upstream's dual-auth `client.api_key = None` fix. |
| `agent/auxiliary_client.py` | merged | R1: kept ours orjson + added explicit `encoding="utf-8", errors="replace"` (P-051 pin). R2: **kept ours P-039 CN vision order (Anthropic-only implicit fallback, no OpenRouter/Nous/DeepInfra probing)** and re-added upstream's MoA-preset unwrap + `main_provider_skipped_for_vision` (indentation adapted to upstream's 8-space base). |
| `agent/background_review.py` | merged | Import region: kept ours `import orjson` (used in file) + upstream `import copy`; dropped unused stdlib `json`. |
| `agent/chat_completion_helpers.py` | merged | R1: imports merged (orjson+contextvars+json). R2: kept ours `_PROVIDER_STATUS_PLATFORMS` + upstream `_context_thread_target`. R3: upstream (ContextVars-bound thread target). R4: **kept ours P-022 bounded escalation** (synthesized TimeoutError, deliberately no last-chunk-time reset per P-022 sync note). |
| `agent/codex_responses_adapter.py` | merged | R1: imports merged (ours `re_compat` + upstream hashlib/json/unicodedata). R2: upstream `deterministic_call_id` delegation (matches P-024 message_sanitization owner) + upstream `_clamp_responses_call_id`. |
| `agent/coding_context.py` | merged | R1: kept ours `from agent.re_compat import re`; dropped `import subprocess` (no longer used). R2: **upstream `bounded_git_probe`** — covers our P-051 utf-8 pins + P-038 hidden-window creationflags + adds deadlock-safe post-kill cleanup (strictly better, preserves CN behavior). |
| `agent/context_compressor.py` | merged | R1: docstring merge (ours `mode:` + upstream `memory_context:`). R2: upstream pre-LLM feasibility-skip structure + `mode=mode` re-added to `_generate_summary` call (ours' compaction-mode feature kept). R3: merged ours post-compaction `gc.collect()` + upstream `trim_memory` + micro-compact reset + ours' `get_tool_schemas`/`handle_tool_call`. |
| `agent/context_references.py` | took-upstream | 2 regions, quote-style only. |
| `agent/conversation_compression.py` | merged | R1/R2: signature+docstring merge (ours `mode` + upstream `defer_context_engine_notification`/`commit_fence`). R3: upstream (lock-skip/ownership re-validation supersedes our 3-line comment). R4: upstream (`commit_fence.finish_commit()` finally). |
| `agent/conversation_loop.py` | merged | 35 regions. Mostly upstream (compact formatting, `estimate_request_tokens_rough`, status templates, lazy logging, redirect-preservation, `_canonicalize_api_tool_calls`, empty-partial-stub guard). R1: upstream (dropped dead `IterationBudget`/`build_memory_context_block` imports). R2: merged both billing/credits hint lines. R4: merged ours P-017 `_prev_dedup_calls` init + upstream redirect drain. R9/R31: upstream (supersets incl. ours' reset logic inside else + stale-marker discard). R33: upstream structure + re-added ours' `_reminder_registry.on_context_compacted` notify. R3/R35: upstream `lifecycle.invoke_hook`. |
| `agent/copilot_acp_client.py` | kept-ours | P-051 pin with explanatory comment (identical behavior to upstream, comment kept). |
| `agent/lsp/client.py` | kept-ours | P-038 `_subprocess_kwargs` (windows_detach_flags_without_breakaway on win32 / start_new_session on POSIX); upstream's version would drop the Windows detach. |
| `agent/lsp/install.py` | merged | R1: all three imports merged (resolve_node_command + windows_hide_flags + find_node_executable — all used per P-038). R2: kept ours defensive HERMES_HOME resolution (frozen-runtime safe). |
| `agent/message_sanitization.py` | merged | Import: kept ours `orjson` (used by our JSON-repair code) + upstream `hashlib` (used by their new code). |
| `agent/moa_loop.py` | merged | Ours orjson serialization adapted to upstream's `fn_args` variable rename (avoids NameError). |
| `agent/model_metadata.py` | merged | R1: merged both gates (`_endpoint_reachable` + `_endpoint_blackholed`). R2: merged ours IncrementalTokenEstimator fast path + upstream per-message memoized fallback. R3: merged ours shadow-dict fast path (with `api_content` guard added for correctness) + upstream `sidecar_wins` handling. |
| `agent/models_dev.py` | kept-ours (adapted) | **P-028 offline-first**: kept our snapshot/allow_network/prewarm machinery wholesale; upstream's singleflight/backoff refactor not adopted (our `_serve_offline_fallback` + prewarm cover the failure path; noted for test-phase re-evaluation). Upstream's additive `get_provider_info`/`_parse_provider_info` retained via shared context. Verified: compiles + imports. |
| `agent/nous_rate_guard.py` | merged | Ours orjson write + upstream `encoding="utf-8"` on fdopen. |
| `agent/prompt_builder.py` | merged | R1: upstream STEER_MARKER machinery (additive; old "User injection prompt" comment obsolete). R2: merged `_REMOTE_TERMINAL_BACKENDS` (ours + upstream `vercel_sandbox`). R3: merged `docker_shm_size` + `docker_persist_across_processes`. R4/R5: merged ours `skills_by_category` aggregation + upstream `visible_entries.append` (both consumers exist in shared context). |
| `agent/secret_scope.py` | merged | `utf-8-sig` (BOM) + `errors="replace"` (P-051) combined. |
| `agent/secret_sources/bitwarden.py` | took-upstream | 2 regions, quote-style only. |
| `agent/shell_hooks.py` | merged | R1 quote-style → upstream; R2 kept ours orjson + explicit utf-8 pin. |
| `agent/skill_preprocessing.py` | took-upstream | Quote-style only. |
| `agent/system_prompt.py` | merged | Import: ours `orjson` (used) + upstream `logging` (used); stdlib `json` unused, dropped. |
| `agent/tool_executor.py` | merged | 36 regions. Structural regions R2–R8, R10–R17, R20–R27, R30–R32, R35–R36: upstream (shared context is upstream-architecture: `_scope_block` 6-tuples, `_managed_values`, `_execution_blocked`, `_ce_result`; upstream's parse-loop append + `_append_cancelled_tool_results` etc.). R9: merged upstream `managed` unpack + ours P-017 dedup tracker check. R18/R28/R29/R33: **kept ours** (P-041 tool_progress_callback/tool_complete_callback events, `_flush_session_db_after_tool_progress`, steer drain, agent_swarm dispatch — shared context's `_swarm_result`/callback vars require ours). R19: merged ours docstring continuation + upstream `_append_cancelled_tool_results` definition (called at 2 shared sites). R34: merged ours `tool_complete_callback` + upstream lazy logging. R1: imports merged (dataclass + Path). |
| `agent/transports/codex.py` | merged | R1: imports (ours orjson + upstream re). R2: ours orjson serialization + upstream `scope_id` cache-scope separator. |
| `agent/transports/codex_app_server.py` | took-upstream | Quote-style only. |
| `agent/turn_context.py` | merged | Both sides' new attribute initializations merged (ours `_preflight_estimator` + upstream `_preflight_compression_blocked`/`_turn_received_provider_response`/`_turn_preflight_display_snapshot`). |
| `agent/turn_finalizer.py` | took-upstream | 4 regions: `hermes_cli.lifecycle.invoke_hook` (new dispatch home; wraps `plugins.invoke_hook` + observability — no CN functionality lost). |
| `apps/desktop/electron/main.ts` | kept-ours | **P-050**: kept ours simplified `findGitBash` (no HERMES_GIT_BASH_PATH / PortableGit candidates); removed now-unused `_findGitBash` import. Upstream's new `find-git-bash.ts` module left untouched (not in batch). |
| `batch_runner.py` | merged | R1 quote-style → upstream; R2 ours orjson write + upstream `flush()`/`fsync()` durability. |

## Notes for the test phase
- `agent/models_dev.py`: upstream's singleflight+5-min-backoff refresh was deliberately not adopted (kept P-028 offline-first design). If upstream tests assert backoff behavior, re-evaluate.
- `agent/agent_runtime_helpers.py`: `repair_empty_non_final_messages` is defined (upstream) and now also invoked at the top of our fused P-024 sanitizer; our P-024 empty-content drop still applies to final payload-less empties.
- `agent/tool_executor.py`: our block-evaluation/checkpoint-preflight pass in the parse loop (old R6/R7) was superseded by upstream's `_scope_block`-tuple design; guardrail/checkpoint logic still present in shared context (lines ~498-517, ~663-681). P-017 dedup, P-041 progress/complete callbacks, steer drain, and agent_swarm dispatch preserved.
- `agent/prompt_builder.py`: upstream STEER_MARKER helpers kept even though our fork's conversation_loop still uses the "User injection prompt:" text path — helpers are additive and used by upstream's steer code.
