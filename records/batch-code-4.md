# Batch code-4 — conflict resolution report

Batch: `batches/code-4.txt` (34 production files: plugins/memory/*, plugins/platforms/*, run_agent.py, tools/*). Resolved per `records/RESOLUTION-PROTOCOL.md` + `FORK_NOTES.md` (CN patches P-013/P-016/P-019/P-030/P-033/P-037/P-038/P-044/P-048/P-050/P-052 and fork perf .plans). All 34 `.py` files pass `python -m py_compile`; **zero conflict markers remain** in every file; `git add` done per file; **no commit** made. Note: the batch file has CRLF line endings and no backticks despite the task sheet quoting.

| File | Decision | Rationale |
|---|---|---|
| `plugins/memory/mem0/__init__.py` | merged | 1 region: kept ours `orjson.loads` (orjson is the file's serializer; upstream's `json` import is absent from the merged file) + added upstream's `encoding="utf-8"` pin (P-051-style). Frozen-runtime `is_available()` fallback (find_spec) already present in shared context. |
| `plugins/memory/mem0/_setup.py` | merged | 3 regions: (1)+(2) kept ours `orjson.dumps/loads` with upstream's `encoding="utf-8"` pin (json not imported by our side); (3) quote-style (`"utf-8"` vs `'utf-8'`) → upstream formatting. |
| `plugins/memory/openviking/__init__.py` | merged | R1 imports: merged all of `errno`, `json`, `orjson` (each used in shared context: errno at `_LOCK_BUSY_ERRNOS`, orjson 12×, json 15×) + kept `agent.re_compat` re. R2 sync_turn trace: took upstream's per-message trace (logs the individually-posted message; ours' batch-payload trace was wrong for the per-message path). |
| `plugins/memory/supermemory/__init__.py` | merged | `is_available()`: kept ours' PyInstaller frozen-runtime fallback (`importlib.util.find_spec` when `tools.lazy_deps` is absent — CN Desktop, mirrors mem0/honcho) + adopted upstream's `get_secret("SUPERMEMORY_API_KEY", "")` (scope-aware read, supersedes our bare `os.environ` check). |
| `plugins/platforms/discord/adapter.py` | merged | 1 block (voice-message payload): kept ours `import orjson as _json` + upstream's `message_reference`/`reply_to` support (`reference` is in scope via `_reply_reference_for_send`); `.decode('utf-8')` retained because orjson returns bytes. |
| `plugins/platforms/google_chat/adapter.py` | merged | 2 regions: kept ours `orjson` (file imports orjson, not json) + upstream's `encoding="utf-8"` read pin and compact-separator write intent (orjson already compact). |
| `plugins/platforms/google_chat/oauth.py` | merged | 2 regions: kept ours `orjson.loads`/`orjson.JSONDecodeError` + upstream's `encoding="utf-8"` pins (orjson imported, json not). |
| `plugins/platforms/matrix/adapter.py` | merged | 1 import region: kept fork's `from agent.re_compat import re` (fork-wide regex shim) + added upstream's `shutil`/`subprocess`/`sys` (all used in shared code). |
| `plugins/platforms/photon/adapter.py` | merged | 4 regions: R1 took upstream's `compile_mention_patterns(...)` shared-helper refactor (`gateway/platforms/helpers.py` exists, is clean, and its import is already at line 66 — same behavior as ours' inline impl); R2–R4 quote-style only → upstream formatting. |
| `plugins/platforms/photon/auth.py` | merged | R1 imports: merged `json` (required by upstream's `_save_auth` continuation `json.dump`), `orjson` (ours, used in `_load_auth`), upstream's `stat`/`uuid`, kept `re_compat` + `pybase64`. R2 `_save_auth`: took upstream's secure per-process `os.open(O_EXCL)` 0o600 temp write — the shared tail (`os.fdopen(fd, ...)`) requires `fd`, ours' block would NameError; upstream's is also a security fix (#19673/#21148). |
| `plugins/platforms/slack/adapter.py` | merged | R1 imports: merged `inspect`/`json`/`orjson` (all used: inspect 2×, json 7×, orjson 3×) + kept `re_compat`. R2 token load: kept ours `orjson.loads(..., errors="replace")` + upstream's `warn_if_credential_file_broadly_readable` security warning (`utils.py` has the helper, clean). |
| `plugins/platforms/sms/adapter.py` | merged | 1 region (Twilio out-of-process send): kept ours `import pybase64 as base64` (fork serializer preference) + upstream's `_get_scoped_secret("TWILIO_ACCOUNT_SID", "")` (scope-aware read, consistent with this file's `_get_scoped_secret` usage for AUTH_TOKEN). |
| `plugins/platforms/telegram/adapter.py` | merged | R1 imports: kept P-038 `hermes_cli._subprocess_compat` (IS_WINDOWS/windows_hide_flags — telegram is a listed P-038 target) + upstream `_coerce_allow_set` (used 2×). R2 `_reload_dm_topics_from_config`: took upstream's canonical `load_config_readonly()` loader (honors managed-scope overlay + `${VAR}` expansion; supersedes our raw yaml read). |
| `plugins/platforms/whatsapp/adapter.py` | merged | 6 regions: 5 quote-style → upstream formatting; 1 region (taskkill) **kept ours** — the `# taskkill prints in the OEM/ANSI codepage (GBK on zh-CN Windows) — locale decode + replace, never strict` comment + `text=True, errors="replace"` (deliberate GBK/cp936 decode, no utf-8 pin). |
| `run_agent.py` | merged | 21 regions. R1: kept ours `gc_init_freeze()` wrapper (.plans/14 perf) + `_init_swarm_mode`/`swarm_mode` (called by `hermes_cli/cli_commands_mixin.py:2130`), upstream's tool_delay DeprecationWarning + fuller `init_agent` kwargs (`requested_provider`, `reaction_callback`; `tool_delay` dropped — `agent/agent_init.py` no longer accepts it). R2–R3: LM Studio — took upstream's `_effective_lmstudio_context_length`/`_lmstudio_load_was_unverified` helpers + kept ours' compressor `update_model` push with safe `ctx_len` extraction, returning the `return_load_result` object. R4 `clear_interrupt`: merged — kept ours `registry.clear_user_reminders()` (registry-based steer queue, P-023) but **dropped** our read-only `_pending_steer` property (merged `agent_init.py:881` assigns `_pending_steer` as a plain attribute — a property would raise AttributeError at agent construction; tests also assign it) and kept upstream's `_pending_steer_lock` null + `return True`. R5 append_messages_batch: took upstream's `messages=_batch_rows` API (`hermes_state.py:6444` signature confirmed). R6 kanban: upstream import (shared tail calls `inject_new_comments_from_env`). R7 credits latch: upstream `new_credits_latch()` factory (exists in `agent/credits_tracker.py:181`). R8 tool_call_id: upstream `_sanitize_coalesce_tool_call_id` (imported at line 234; P-024 audit F4 owner). R9–R10 request-client cache: upstream `_REQUEST_CLIENT_REUSE_REASONS`/`_request_client_cache_ref`/wire-client reuse — the shared `_close_request_openai_client` tail depends on these structures (also a perf win aligned with fork goals). R11–R12 credential swap: upstream `_credential_pool_entry_id`/`normalize_route_base_url`/`route_changed` (shared tail `_reapply_route_client_config(route_changed=...)` requires it) + `self._anthropic_base_url` arg. R13 `_build_api_kwargs`: upstream `tools_for_api=tools_for_api`. R14 `_supports_reasoning_extra_body`: upstream adds `ai-gateway.vercel.sh`. R15 `_sanitize_tool_call_arguments`: upstream `cursor=cursor`. R16–R17 `_compress_context`: merged both signatures (`mode` ours + `defer_context_engine_notification`/`commit_fence` upstream), upstream import set, and re-added `mode=mode` to the inner `compress_context` call (merged `agent/conversation_compression.py:2140` still accepts `mode` — fork compaction mode preserved). R18: kept ours `_dispatch_agent_swarm` (called by `agent/agent_runtime_helpers.py:3127` and `tools/tool_executor.py:1846`) + upstream `_invoke_tool` signature with `skip_tool_execution_middleware`. |
| `tools/browser_tool.py` | took-upstream | 1 region, quote-style only (`"utf-8"` vs `'utf-8'`). |
| `tools/checkpoint_manager.py` | took-upstream | 3 regions: B1–B2 quote-style; B3 orphan-repo deletion took upstream's `repo["marker_unreadable"]`/`_workdir_is_observably_gone`/`orphan_allowlist` condition — the shared context fully wires it (`_pre_v2_shadow_repos` returns those keys at line 644; `_workdir_is_observably_gone` at 1391; `orphan_allowlist` param at 1496); ours' inline HERMES_WORKDIR marker read is superseded by the helper refactor. |
| `tools/clarify_tool.py` | merged | B1+B2: upstream `tool_error(...)` (orjson-backed, identical JSON shape). B3: kept ours (`"user_response": str(user_response).strip(), }).decode('utf-8')`) — the shared prefix is `return orjson.dumps({`, upstream's `ensure_ascii=False` closing belongs to a `json.dumps` call that isn't open. |
| `tools/close_terminal_tool.py` | kept-ours | Import region: kept `import orjson` (used at line 34); dropped ours' unused `import os` and upstream's unused `import json`. |
| `tools/code_execution_tool.py` | merged | B1 imports: kept **P-044** `from platform_utils import is_windows` (WMI-free; `_IS_WINDOWS = is_windows()` at line 53) + added upstream's `import re` (used 5×); dropped unused `import platform`. B2–B6: upstream `tool_error(...)` (orjson-backed, identical shape). |
| `tools/computer_use/cua_backend.py` | merged | B1 imports: merged `pybase64 as base64` (fork preference, provides the `base64` name used 2×) + `deque`/`functools` (upstream, used 2× each) + `orjson` (ours, used 4×). B2: upstream `driver_cmd` (resolved var in scope). B3: upstream `creationflags=windows_hide_flags()` + `child_env` (defined in `_call_tool_via_cli`). |
| `tools/computer_use/doctor.py` | took-upstream | 4 regions (git-misaligned across functions). B1 `_drive_health_report`: upstream `_mcp_rpc` helper (shared context defines it; tail uses `call_resp`). B2 `_cli_driver_version`: upstream `--version` subprocess probe (ours' block was misaligned junk referencing `call_line`; tail uses `completed`). B3: upstream version regex (ours' structuredContent parsing is fully covered by upstream's `_extract_health_report_from_result` at line 153). B4 `run_doctor`: upstream additive `hermes_identity` envelope (preserves report keys). `orjson` import kept — still used at line 851. |
| `tools/computer_use/permissions.py` | took-upstream | 1 region, quote-style only. |
| `tools/computer_use/tool.py` | merged | B1 imports: merged `atexit` (upstream, 9×) + `pybase64 as base64` (ours). B2 missing-action: kept ours orjson (identical behavior). B3 denied: merged both — upstream's `verdict == "timeout"` "silence is not consent" case + ours' orjson denied-by-user return. B4 `_action_payload`: upstream (returns dict + `verdict=_classify_action_result(res)`; the shared `_text_response` does `json.dumps(_action_payload(res))` so a dict return is required). B5: upstream `data.update(_action_payload(res))` + `json.dumps(data)` (data parsed via shared `orjson.loads(resp)`). |
| `tools/delegate_tool.py` | merged | B1 imports: merged `contextvars`/`json` (upstream, used) + `orjson` (ours, used 11×). B2 child-agent construction: took upstream's complete `with delegated_child_context(): child = AIAgent(...)` call — ours' block was an orphaned kwargs fragment (the `child = AIAgent(` head moved into upstream's block); upstream's call includes every one of our kwargs. B3: upstream `tool_error(...)`. |
| `tools/discord_tool.py` | took-upstream | 4 regions, all ours-orjson → upstream `tool_error(...)` (orjson-backed, identical shape; also adds `_enrich_403`/logging parity). |
| `tools/environments/base.py` | took-upstream | 2 regions (snapshot temp-name generation): upstream `mktemp` template + `snapshot_excluded` — the shared tails reference `_snap_tmp_template` and `snapshot_excluded`, which only upstream's block defines; ours' Python-UUID variant would NameError. |
| `tools/environments/docker.py` | merged | B1 imports: merged `hashlib`/`json`/`shlex` (upstream, each used) + `orjson` (ours, used) + kept `re_compat` re. B2–B14: 13 quote-style regions → upstream formatting. |
| `tools/environments/local.py` | took-upstream | Only 2 regions, both trivial kwarg formatting (`text=True,\n encoding=...` vs single-line) → upstream formatting. **All CN machinery intact** (verified): `_find_powershell`/`_wrap_command_powershell`/`pwsh_transform` (P-016/P-019/P-037), `refresh_env_from_registry` (P-020/P-042), `PowerShellSession` (P-042), `_find_bash_posix`/`_with_msystem_neutralized` (P-050/P-052), `_find_pwsh` (P-XXX). |
| `tools/environments/singularity.py` | took-upstream | 4 regions, quote-style only (ours' `windows-footgun` comment on B3 was dropped with the block — note for test phase if the comment matters). |
| `tools/environments/ssh.py` | took-upstream | 7 regions, quote-style only. |
| `tools/file_operations.py` | merged | 9 regions. R1 imports: merged `fnmatch` (ours, P-030 in-process search uses it) + `hashlib` (upstream sha256 verify). R2 TOML check: upstream (py3.14+ always has tomllib; P-048 makes the tomli fallback dead code). R3 has-BOM head read: **kept ours** `_prim_read_sample` (P-033 — `head -c 3` shell can't run under PS 5.1). R4 patch_replace pre_content: merged — upstream's caller-reuse structure + ours' `_prim_read_all` (P-037: `cat` shell-out broken on PS 5.1). R5 dirs_created: **kept ours** `_prim_mkdirs` in-process (P-033). R6 post-write verify: merged both — ours' `_prim_stat_size` size check (in-process, works everywhere) + upstream's sha256 content verification (`content_verified` required by the shared `WriteResult(verified=...)` tail; `sha256sum` fails gracefully to None on PS). R7 search path validation: merged — ours' local in-process `os.path.exists` (P-030; `test -e` broken under PowerShell) + upstream's multi-path recovery for remote backends. R8 content search: **kept ours** local branch (`_find_rg` + `_search_content_python`, P-030/P-049) + upstream's `used_rg = False` init (shared tail requires it). R9 parse output: kept ours `_parse_search_content_output` helper (shared, used at 3 sites; multiline `-U` note covered by `_maybe_warn_line_oriented_newline_pattern`). |
| `tools/file_tools.py` | took-upstream | 7 regions, all ours-orjson → upstream `tool_error(...)` (verified: `tools/registry.py:1348` `tool_error` itself uses `orjson.dumps(...).decode('utf-8')` — identical serializer and JSON shape; blocks also carry upstream's negative-result-cache comments). |
| `tools/fuzzy_match.py` | took-upstream | 1 region (block match loop): upstream's `_sim`-based strict all-lines check with blank-pattern-line skip — `_sim` and `all_match` are wired in the shared context (anchors at lines 836/838 already use `_sim`), consistent; ours' `_fuzz`-ratio 50% majority variant dropped (rapidfuzz `_fuzz` import kept — still used at lines 783/1053). |

## Notes for the merge record / test phase
- `run_agent.py` R4: the read-only `_pending_steer` property was intentionally dropped so the plain-attribute initialization in the merged `agent/agent_init.py` (line 881) and `tests/run_agent/test_steer.py` (which assign `agent._pending_steer = None`) do not raise `AttributeError`. Registry-based steer clearing (`registry.clear_user_reminders()`) is preserved in `clear_interrupt`; `steer()`/`_drain_pending_steer()` still use the reminder registry.
- `tools/file_operations.py` R6: upstream's sha256 verify shells out to `sha256sum`; on Windows PowerShell this fails gracefully (`content_verified=None`) — our in-process `_prim_stat_size` size check is the primary verification there.
- `plugins/platforms/whatsapp/adapter.py`: taskkill output decode deliberately keeps locale (GBK) decoding — do not "fix" to `utf-8`.
- All 34 files: 0 conflict markers, `py_compile` green, staged via `git add`. No commit / no merge-state changes made.
